[project]
name = "model-price-backend"
version = "0.1.0"
description = "AI Model Pricing API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn[standard]>=0.40.0",
    "httpx>=0.28.0",
    "pyyaml>=6.0",
]
